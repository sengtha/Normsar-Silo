# Generated by bootstrap.sh — contains secrets, never commit.
.env
# Caddy/Postgres data live in named Docker volumes, but ignore any local bind dirs.
volumes/db/data/
volumes/storage/
volumes/caddy/data/
